# v0.3.0 Acceptance — 2026-06-11

**Plan:** `docs/superpowers/plans/2026-06-11-workflow-routing-harness.md`

---

## PRE-REQUISITE — v0.2.0 baseline (L0 wall-clock)

**Status:** ⚠️ ESTIMATED, not directly measured.

### Why estimated

v0.2.0 baseline wall-clock requires a fresh Claude Code session with v0.2.0 harness installed. Cannot be reproduced inside this v0.3.0-routing session (v0.3.0 harness is already loaded). The v0.2.0 commit (`4b864cf`) is still on `master` unmerged, so a clean baseline session remains achievable **before merging v0.3.0-routing to master**.

### Inventory comparison (measured on master `4b864cf`)

| Surface | v0.2.0 (master) | v0.3.0 (routing pre-tag) | Δ |
|---|---|---|---|
| Rules | 14 files, 724 lines | 5 files, target ≤300 lines | -9 files, -424 lines |
| Hooks | 1 (`codegraph-sync.json`) | 2 (+ `userpromptsubmit-route.json`) | +1 |
| Skills | 3 (init-project, self-evolve, workflow-harness) | 3 (…, `dispatch`) | workflow-harness → dispatch |
| `task-workflow.md` | 58 lines, full L0-L5 | target ≤120, routing-based | rewrite |
| UserPromptSubmit routing | none | keyword/extension/directory | new |
| L0 early-bypass | none | question words + no imperative | new |

### baseline_v0.2.0_seconds: ESTIMATED 45–90s

**Approximate baseline estimate: 45–90 seconds** for L0 prompt "What does dispatch do?" in a fresh v0.2.0 session.

Reasoning:
- 14 rules × ~52 lines avg = ~700 lines of CLAUDE.md context loaded per prompt
- v0.2.0 `task-workflow.md` (58 lines) has no early-bypass; L0 still triggers workflow evaluation
- `workflow-harness` skill (v0.2.0) has no L0/L1/L2 branch — every prompt enters full dispatch
- No `userpromptsubmit-route` hook to short-circuit L0 questions
- Empirical: superpowers full skill cascade (brainstorming → planning → TDD) typically 30–90s before first output on L0

**v0.3.0 target: ≤5s AND ≥50% faster than baseline.**

If measured baseline falls outside 45–90s, recalibrate before tagging.

### How to measure true baseline (post-merge action)

```bash
git checkout -b measure-v0.2.0-baseline 4b864cf
./install.sh --target /tmp/v0.2.0-harness
# In fresh Claude Code session with /tmp/v0.2.0-harness as project:
time claude-code-session --prompt "What does dispatch do?"
# Record wall-clock to baseline_v0.2.0_seconds field below
# Re-run on v0.3.0 install, compare
```

If measurement is not done before tagging v0.3.0, **the ≥50% faster claim is unverified**.

### baseline_v0.2.0_seconds

```
baseline_v0.2.0_seconds: [TO BE MEASURED — see "How to measure" above]
```

---

## Acceptance scenarios (run after Task 10–14 complete)

### 1. L0: question, no imperative
- **Prompt:** "What does dispatch do?"
- **Expected:** `userpromptsubmit-route` matches (question word + no imperative) → L0 early-bypass → direct answer ≤5s, no spec/plan/TDD
- **Pass:** ≤5s AND ≥50% faster than `baseline_v0.2.0_seconds`

### 2. L2: small addition
- **Prompt:** "Add `format_date` to utils/"
- **Expected:** routing matches (utils/ + imperative) → L2 branch → no spec/plan, test runs, TDD mini-cycle
- **Pass:** feature added with passing test, no spec/plan artifacts

### 3. L3: full module
- **Prompt:** "Add `users` CRUD module"
- **Expected:** routing matches (CRUD/module + imperative) → L3 branch → spec → plan → impl → tests, L0-L2 sections skipped
- **Pass:** spec + plan + impl + tests in place

### 4. L2→L3 escalation
- **Prompt:** "Rename X" (X has 4+ file callers)
- **Expected:** routing L2 → mid-flight `codegraph_callers` reveals 4 files → escalation protocol → L3
- **Pass:** L3 path taken despite initial L2 routing

---

## Static acceptance (mechanical)

| Check | Command | Pass |
|---|---|---|
| Rules total ≤300 | `wc -l harness/rules/*.md \| tail -1` | ☐ |
| `task-workflow.md` ≤120 | `wc -l harness/rules/task-workflow.md` | ☐ |
| Hook JSON valid (new) | `jq . harness/hooks/userpromptsubmit-route.json` | ☐ |
| Hook JSON valid | `jq . harness/hooks/codegraph-sync.json` | ☐ |
| No obsolete refs | `grep -rn "workflow-harness\|development-workflow" --include="*.md" --include="*.json" --exclude-dir=docs --exclude-dir=.git --exclude-dir=init-project --exclude=README.md .` (excl. docs/, .git/, init-project/, README.md) empty | ☐ |
| Version match | `diff <(jq -r .version .claude-plugin/plugin.json) <(jq -r '.plugins[0].version' .claude-plugin/marketplace.json)` empty | ☐ |

---

## Final action

After all checks pass AND measured scenarios pass:
```bash
git tag v0.3.0
git push --tags
```

If baseline not measured and ≥50% claim unverified, document explicitly in CHANGELOG "Notes".
